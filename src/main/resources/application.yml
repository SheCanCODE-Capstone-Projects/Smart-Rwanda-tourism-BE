spring:
  application:
    name: smart-rwanda-tourism
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/smart_rwanda_tourism}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:@Chadia0799321180}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


  mail:
    host: smtp.gmail.com
    port: 587
    username: ingabirechadia2003@gmail.com
    password: jxrzrnaiuoxpfrpu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: ${SERVER_PORT:8080}


jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}


logging:
  level:
    org.springframework.security: DEBUG
    com.smartrwanda.tourism: DEBUG